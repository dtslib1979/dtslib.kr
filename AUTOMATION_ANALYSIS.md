# 🔍 Global Automation Analysis Report

## 1. WRITERS_TABLE — 파일별 쓰기 주체 목록

### 📁 **Target Files & Their Writers**

| Target File(s) | Writer (Workflow/Script) | Trigger | Reads From | Writes To | Priority |
|---------------|--------------------------|---------|------------|-----------|----------|
| `assets/home.json` | `category-index.yml` → `generate_category_index.py` | push to `category/**/*.html`, workflow_dispatch | `category/*/*.html` (file count) | `assets/home.json` | **HIGH** |
| `category/*/index.html` | `pages-maintenance.yml` (bash AWK script) | push to `category/**.html`, `index.html`, `assets/**` | `category/*/index.html` (existing), `find` command | `category/*/index.html` | **HIGH - OVERWRITES** |
| `assets/manifest.json` | `build-textstory.yml` → `auto_install.py` | push to `backup/**`, `scripts/**`, `.github/workflows/**`, schedule (every 3h) | `backup/*.html`, `archive/*.html` | `assets/manifest.json`, `archive/*.html` | **CRITICAL** |
| `archive/*.html` | `build-textstory.yml` → `auto_install.py` | same as above | `backup/*.html` | `archive/*.html` | MEDIUM |
| `archive/*.html` | `mobilize.yml` → `clean_and_mobilize.py` | schedule (every 6h), push to `backup/raw/*.html` | `backup/raw/*.html` or `archive/*.html` | `archive/*.html` | MEDIUM |
| `sitemap.xml` | `pages-maintenance.yml` | push to `category/**.html`, `index.html` | `find . -name "*.html"` | `sitemap.xml` | LOW |
| `_obsidian/_imports/` | `obsidian-sync.yml` | push to `category/**`, schedule (every 3h) | `category/**` | `_obsidian/_imports/` | LOW |
| `_obsidian/_imports/category/` | `obsidian-backup.yml` | push to `category/**` | `category/**` | `_obsidian/_imports/category/` | LOW |
| `backup/raw/*.html` | `tistory-backup.yml` → `tistory_backup.py` | schedule (every 6h), workflow_dispatch | Tistory RSS feed | `backup/raw/*.html` | LOW |

---

## 2. OVERWRITE_GRAPH — 광역 덮어쓰기 맵

### 🔄 **Push to `category/<Persona>/*.html` 시 실행 순서**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  USER PUSH: category/<Persona>/new-page.html                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌───────────────────────────────────────────────────────────────┐
    │  PARALLEL TRIGGERS (모두 동시에 트리거됨!)                      │
    └───────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ category-index  │  │ pages-maintenance │  │ obsidian-backup │
│     .yml        │  │      .yml         │  │     .yml        │
└─────────────────┘  └─────────────────┘  └─────────────────┘
         │                    │                    │
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ generate_       │  │ AWK script in   │  │ rsync category/ │
│ category_       │  │ workflow (bash) │  │ to _obsidian/   │
│ index.py        │  │                 │  │                 │
└─────────────────┘  └─────────────────┘  └─────────────────┘
         │                    │                    │
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────────────────────────────────┐
│ assets/home.json│  │  category/*/index.html                      │
│ (카운트만 저장) │  │  ⚠️ <ul id="post-list"> 내용 덮어쓰기!       │
└─────────────────┘  └─────────────────────────────────────────────┘
                                    │
                                    │
                                    ▼
                     ┌─────────────────────────────────────────┐
                     │  🔴 CONFLICT ZONE                        │
                     │  pages-maintenance.yml이 AWK로           │
                     │  category/*/index.html을 다시 생성       │
                     │  → generate_category_index.py가          │
                     │    index.html을 수정하지 않으므로         │
                     │    직접적인 충돌은 없음                   │
                     └─────────────────────────────────────────┘
```

### 🕐 **Schedule 기반 자동화 (3시간/6시간 주기)**

```
┌────────────────────────────────────────────────────────────────────────┐
│  SCHEDULED JOBS (독립적으로 실행됨)                                    │
└────────────────────────────────────────────────────────────────────────┘
         │
         ├── Every 3 hours ──────────────────────────────────────────────┐
         │   ┌─────────────────────────────────────────┐                 │
         │   │ build-textstory.yml                     │                 │
         │   │ → auto_install.py                       │                 │
         │   │                                         │                 │
         │   │ 📖 READS:  backup/*.html                │                 │
         │   │ ✏️ WRITES: archive/*.html               │                 │
         │   │           assets/manifest.json          │                 │
         │   │                                         │                 │
         │   │ ⚠️ assets/manifest.json은 archive/     │                 │
         │   │    기반으로만 생성됨                    │                 │
         │   │    → category/ 폴더는 무시됨!          │                 │
         │   └─────────────────────────────────────────┘                 │
         │                                                               │
         │   ┌─────────────────────────────────────────┐                 │
         │   │ obsidian-sync.yml                       │                 │
         │   │ → rsync + pandoc                        │                 │
         │   │                                         │                 │
         │   │ 📖 READS:  category/**                  │                 │
         │   │ ✏️ WRITES: _obsidian/_imports/          │                 │
         │   │           (category와 무관한 경로)      │                 │
         │   └─────────────────────────────────────────┘                 │
         │                                                               │
         ├── Every 6 hours ──────────────────────────────────────────────┤
         │   ┌─────────────────────────────────────────┐                 │
         │   │ mobilize.yml                            │                 │
         │   │ → clean_and_mobilize.py                 │                 │
         │   │                                         │                 │
         │   │ 📖 READS:  backup/raw/ or archive/      │                 │
         │   │ ✏️ WRITES: archive/*.html               │                 │
         │   │           (category와 무관)             │                 │
         │   └─────────────────────────────────────────┘                 │
         │                                                               │
         │   ┌─────────────────────────────────────────┐                 │
         │   │ tistory-backup.yml                      │                 │
         │   │ → tistory_backup.py                     │                 │
         │   │                                         │                 │
         │   │ 📖 READS:  Tistory RSS                  │                 │
         │   │ ✏️ WRITES: backup/raw/*.html            │                 │
         │   │           (category와 무관)             │                 │
         │   └─────────────────────────────────────────┘                 │
         │                                                               │
         └───────────────────────────────────────────────────────────────┘
```

---

## 3. ANSWER_YES_OR_NO — 상위 자동화가 덮어쓰고 있는가?

### ❌ **NO, 광역 덮어쓰기는 없고, 파이프라인 설계 문제가 있다.**

#### 📋 요약 (5줄 이내)

1. **`pages-maintenance.yml`이 `category/*/index.html`의 `<ul id="post-list">`를 AWK로 덮어쓰지만**, 이것은 해당 폴더의 HTML 파일을 기반으로 최신순 리스트를 다시 생성하는 것이므로 **정상 동작**이다.

2. **`assets/home.json`은 `generate_category_index.py`가 카테고리별 파일 개수만 저장**하며, 이것은 index.html 생성과 무관하게 별도로 동작한다.

3. **`assets/manifest.json`은 `auto_install.py`가 `archive/` 폴더 기준으로만 생성**하므로, `category/` 폴더의 새 파일은 이 manifest에 반영되지 않는다.

4. **덮어쓰기 문제가 아니라, 설계 분리 문제**: `category/*/manifest.json`과 `assets/manifest.json`이 서로 다른 소스(category vs archive)를 기반으로 독립적으로 동작함.

5. **index.html의 JS는 `manifest.json` 또는 GitHub API를 사용**하며, `assets/home.json`의 카운트와 별개로 동작한다.

#### 🔍 상세 분석

| 파일 | 문제 여부 | 설명 |
|-----|---------|------|
| `category/*/index.html` | ✅ 정상 | `pages-maintenance.yml`이 push마다 최신 HTML 목록으로 갱신 |
| `assets/home.json` | ✅ 정상 | `category-index.yml` → `generate_category_index.py`가 카테고리별 파일 개수 저장 |
| `assets/manifest.json` | ⚠️ 주의 | `archive/` 폴더만 스캔 → `category/` 파일은 무시됨 |
| 메인 `index.html` 카운트 | ✅ 정상 | JS가 `manifest.json` 또는 GitHub API로 동적 로딩 |

#### 🚫 "덮어쓰기"가 아닌 이유

- `pages-maintenance.yml`의 AWK 스크립트는 **해당 폴더의 파일을 기반으로 리스트를 재생성**하므로, 새 파일이 추가되면 리스트에 포함됨
- `generate_category_index.py`는 **index.html을 수정하지 않음** (소스 파일 `scripts/generate_category_index.py` 15행 주석: `# Note: Advanced index.html files are now preserved`)
- 두 워크플로우 간에 **직접적인 파일 충돌은 없음**

---

## 4. MINIMAL_FIX — 최소 수정안

### 🎯 **Option 1: 단일 파이프라인 유지 (권장)**

현재 구조는 이미 잘 분리되어 있음. 문제가 발생한다면 아래를 확인:

#### 1-1. `category-index.yml`의 트리거 확인
```yaml
# 현재 설정 (정상)
on:
  push:
    paths:
      - "category/**/*.html"    # ✅ 새 HTML 파일 추가 시 트리거
      - "scripts/generate_category_index.py"
      - "assets/home.json"
```

**문제 없음**: 새 파일 추가 시 `assets/home.json`이 자동 업데이트됨.

#### 1-2. `pages-maintenance.yml`의 AWK 스크립트 확인
```yaml
# 현재 설정 (정상)
on:
  push:
    paths:
      - 'category/**.html'      # ✅ 새 HTML 파일 추가 시 트리거
      - 'category/**/**.html'
```

**문제 없음**: 새 파일 추가 시 `category/*/index.html`의 리스트가 자동 업데이트됨.

---

### 🎯 **Option 2: 명시적 비활성화 (불필요)**

모든 워크플로우가 서로 다른 파일을 대상으로 하므로, 비활성화할 필요 없음.

| 워크플로우 | 비활성화 필요? | 이유 |
|-----------|--------------|------|
| `category-index.yml` | ❌ 유지 | `assets/home.json` 카운트 업데이트 |
| `pages-maintenance.yml` | ❌ 유지 | `category/*/index.html` 리스트 갱신 |
| `build-textstory.yml` | ❌ 유지 | `archive/` 폴더 전용, category와 무관 |
| `obsidian-sync.yml` | ❌ 유지 | `_obsidian/` 폴더 전용, category와 무관 |

---

### 🔧 **실제로 문제가 발생하는 경우의 해결책**

#### 문제 시나리오 1: "카운트가 업데이트되지 않음"
- **확인**: GitHub Actions 탭 (https://github.com/dtslib1979/UncleParksy/actions) → `🗂️ Category Index Builder` 워크플로우 실행 이력 확인
- **해결**: Actions 탭 → `🗂️ Category Index Builder` 선택 → "Run workflow" 버튼 클릭 또는 `gh workflow run category-index.yml` 명령어 실행

#### 문제 시나리오 2: "index.html 리스트가 비어있음"
- **확인**: `pages-maintenance.yml`의 AWK 스크립트는 `<ul id="post-list">` 태그를 찾아 내용을 교체함. 해당 카테고리의 `index.html` 파일에서 이 태그가 있는지 확인: `grep -n 'post-list' category/<Persona>/index.html`
- **해결**: index.html에 `<ul id="post-list">` 태그가 없다면 추가하거나, 해당 index.html이 `pages-maintenance.yml`의 AWK 스크립트 대상인지 확인 (`.github/workflows/pages-maintenance.yml` 40-84행 참조)

#### 문제 시나리오 3: "manifest.json에 새 파일이 없음"
- **원인**: `assets/manifest.json`은 `archive/` 폴더만 스캔
- **해결**: `category/*/manifest.json`을 사용하거나, `generate_category_index.py`를 수정하여 각 카테고리별 manifest 생성

---

## 📌 결론

**현재 자동화 구조는 정상적으로 설계되어 있으며, 광역 덮어쓰기 문제는 없습니다.**

문제가 발생한다면:
1. GitHub Actions 탭에서 워크플로우 실행 상태 확인
2. 각 워크플로우의 트리거 조건 확인 (push path patterns)
3. 파일 경로가 트리거 패턴과 일치하는지 확인

**추가 개선 제안**:
- 각 `category/*/manifest.json`을 표준화하여 index.html이 일관되게 manifest를 로딩하도록 함
- `generate_category_index.py`를 확장하여 각 카테고리별 manifest.json도 생성하도록 수정 (Optional)
